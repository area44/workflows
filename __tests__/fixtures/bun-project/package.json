{
  "name": "mock-bun-project",
  "scripts": {
    "build": "mkdir -p dist && touch dist/index.html",
    "lint": "echo linting...",
    "format": "echo formatting..."
  },
  "dependencies": {
    "minimist": "^1.2.8"
  }
}
